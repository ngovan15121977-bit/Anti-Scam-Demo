# Core
fastapi>=0.115.0
uvicorn[standard]>=0.34.0
pydantic[email]>=2.5.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
#đã check
# AI / LangChain
langchain>=0.3.0
langchain-openai>=0.3.0
openai>=1.0.0
langgraph>=0.2.0

# Database
sqlalchemy>=2.0.0
# Async SQLite driver used by the application and the in-memory test database.
aiosqlite>=0.20.0
alembic>=1.14.0
psycopg2-binary>=2.9.0
pgvector>=0.3.0

# Data import / auth
email-validator>=2.2.0
python-jose[cryptography]>=3.3.0
google-auth>=2.0.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.20
cloudinary>=1.41.0
pandas>=2.1.0
openpyxl>=3.1.0
numpy>=1.26.0,<2.0

# Lightweight local face verification using OpenCV Zoo SFace + YuNet.
# These models are downloaded lazily on the first enrollment/verification.
Pillow>=10.0.0
# Detect and crop the single primary face before SFace creates an embedding.
opencv-contrib-python-headless==4.10.0.84

# Dev tools
ruff>=0.8.0
pytest>=8.0.0
pytest-asyncio>=0.24.0
pytest-cov>=5.0.0
httpx>=0.28.0
