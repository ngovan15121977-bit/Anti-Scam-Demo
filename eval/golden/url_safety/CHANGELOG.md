# URL safety golden changelog

## 2026-08-23

- Added a safe `example.com` case.
- Added a known blacklist case for `lntbitbank.com`.
- Current CI gate validates the JSONL contract; behavioral URL execution is a
  follow-up item.

